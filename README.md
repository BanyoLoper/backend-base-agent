## Starting the Project
```bash
python -m venv venv
.\venv\Scripts\activate
pip install django djangorestframework django-channels channels-redis daphne
```

## Run Redis by Docker
```bash
# Detached -d
docker compose up -d
```

